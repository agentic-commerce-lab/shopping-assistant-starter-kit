vendor/
node_modules/
composer.lock
.env
.env.local
.DS_Store
.phpunit.result.cache
.phpunit.cache/
.phpcca.cache/
report/
var/

# Playwright MCP scratch output (screenshots, page snapshots)
/.playwright-mcp/

# Playwright run output (dev-only e2e)
/test-results/
/playwright-report/

.superpowers/

# Per-machine Claude Code permissions. Until now this was invisible only because of a
# personal ~/.config/git/ignore entry — a fresh clone showed it as untracked.
.claude/settings.local.json
